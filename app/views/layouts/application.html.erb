<!DOCTYPE html>
<html lang="de">
  <head>
    <title><%= content_for(:title) || "CasaPlan" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="CasaPlan">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-stone-50 text-stone-800">
    <% if authenticated? %>
      <header class="bg-white border-b border-stone-200">
        <nav class="max-w-4xl mx-auto px-4 py-3 flex flex-wrap items-center gap-4">
          <%= link_to "CasaPlan", root_path, class: "font-bold text-teal-700 text-lg mr-4" %>
          <%= link_to "Belegungsplan", stays_path, class: "hover:underline" %>
          <%= link_to "Aktivitäten", activities_path, class: "hover:underline" %>
          <% if policy(User).index? %>
            <%= link_to "Benutzerverwaltung", admin_users_path, class: "hover:underline" %>
          <% end %>
          <span class="ml-auto flex items-center gap-4">
            <%= link_to Current.user.name, profile_path, class: "hover:underline" %>
            <%= button_to "Abmelden", session_path, method: :delete, class: "btn-secondary py-1" %>
          </span>
        </nav>
      </header>
    <% end %>

    <main class="max-w-4xl mx-auto px-4 py-8">
      <div id="flash">
        <% if notice %>
          <p class="mb-6 rounded-md bg-green-50 border border-green-200 px-4 py-3 text-green-800"><%= notice %></p>
        <% end %>
        <% if alert %>
          <p class="mb-6 rounded-md bg-red-50 border border-red-200 px-4 py-3 text-red-800"><%= alert %></p>
        <% end %>
      </div>

      <%= yield %>
    </main>
  </body>
</html>
