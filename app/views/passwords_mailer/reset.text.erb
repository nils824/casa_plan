Hallo <%= @user.name %>

Du kannst dein Passwort für CasaPlan hier zurücksetzen:
<%= edit_password_url(@user.password_reset_token) %>

Der Link ist <%= distance_of_time_in_words(0, @user.password_reset_token_expires_in) %> gültig.
